import { BaseGridState } from '../baseGridState';
import { DataTile } from './dataTile';
export declare function createDataGridState(styleZoom: number, minZoom: number, maxZoom: number, minDetailLevel: number, maxDetailLevel: number): BaseGridState<DataTile>;
