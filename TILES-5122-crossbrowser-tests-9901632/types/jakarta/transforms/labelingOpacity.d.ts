import { MapState } from '../types';
export declare const set: (state: MapState, easing: string, from: number, to: number, duration: number) => void;
export declare const update: (state: MapState) => void;
